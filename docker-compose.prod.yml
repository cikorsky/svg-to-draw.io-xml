services:
  svg-to-drawio:
    image: svg-to-drawio:0.1.0
    container_name: svg-to-drawio
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=3000
